name: freezed_error_demo
description: A sample command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  meta: ^1.1.8

dev_dependencies:
  build_runner:
  pedantic: ^1.8.0
  test: ^1.6.0
  freezed: ^0.1.0
